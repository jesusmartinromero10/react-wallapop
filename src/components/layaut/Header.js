import Button from '../button';

const Header = () => {
  return (
    <header>
      <nav>
        <Button>Loging</Button>
      </nav>
      <div></div>
    </header>
  );
};
export default Header;
